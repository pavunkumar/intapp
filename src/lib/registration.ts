// Authoritative registration fee, in paise. /api/register reads it from
// here — the client never sends an amount.
export const REGISTRATION_FEE_PAISE = 29900;
export const REGISTRATION_FEE_LABEL = "₹299";
